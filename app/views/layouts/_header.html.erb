<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <%= link_to 'YuiKaori', root_path, class: 'navbar-brand' %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

      <!-- 左側のコンポーネント -->
      <ul class="nav navbar-nav">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">MENU <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to 'ゆいかおり', pictures_search_path(column: 'member_id',value: 1) %></li>
            <li><%= link_to '小倉唯', pictures_search_path(column: 'member_id',value: 2) %></li>
            <li><%= link_to '石原夏織', pictures_search_path(column: 'member_id',value: 3) %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to '日付順', pictures_path %></li>
            <li role="separator" class="divider"></li>
              <!-- 検索フォーム -->
            <li>
              <%= form_tag pictures_search_path(column: 'event'), method: :get, class: 'navbar-form navbar-left', role: 'search' do %>
                <div class="form-group">
                  <%= text_field_tag(:value, nil, class: 'form-control', placeholder: 'イベント検索') %>
                </div>
                <%= submit_tag '検索', class: 'btn btn-default' %>
              <% end %>
            </li>
            <li role="separator" class="divider"></li>
            <li>
              <%= form_tag pictures_search_path(column: 'tag'), method: :get, class: 'navbar-form navbar-left', role: 'search' do %>
                <div class="form-group">
                  <%= text_field_tag(:value, nil, class: 'form-control', placeholder: 'タグ検索') %>
                </div>
                <%= submit_tag '検索', class: 'btn btn-default' %>
              <% end %>
            </li>
          </ul>
        </li>
      </ul>

      <!-- 右側のコンポーネント -->
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to current_user.username, toppage_path %></li>
          <li><%= link_to 'modify profile', edit_user_registration_path %></li>
          <li><%= link_to 'log out', destroy_user_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to 'sign up', new_user_registration_path %></li>
          <li><%= link_to 'log in', new_user_session_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->

</nav>
