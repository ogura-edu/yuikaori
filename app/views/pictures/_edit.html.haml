-# 仕方無くtext_fieldにしているが、既存のものは変更できないようにボタン式とかセレクト式にしたい。どちらにしろかさばるからどう表示したものか。。。

= form_for(@picture, remote: true) do |f|
  .modal-body
    .center-block
      = f.collection_select :member_id, Member.all, :id, :member
    .center-block
      = f.fields_for :event do |e|
        = e.text_field :event, placeholder: Event.find(@picture.event_id_was).event
    .center-block
      = f.date_select :date
    .center-block
      = f.fields_for :tags do |t|
        = t.text_field :tag
    .center-block
      = f.url_field :article_url
  .modal-footer
    = f.submit class: 'btn btn-primary'
    %button.btn{aria: {hidden: true}, data: {dismiss: 'modal'}} Close
