$('.modal-form').html("<%= escape_javascript(render 'edit') %>");
$('#notice').html("<% if flash[:notice] %> <%= j(javascript_tag %Q{alert('#{flash.now[:notice]}')}) %> <% end %>");
$('input[data-role=tagsinput]').tagsinput();
$('.new-record').on('click', function(){
  if($(this).hasClass('event')) {
    $('.event-form').append('<input placeholder="イベント名" type="text" name="picture[event_attributes][name]" id="picture_event_attributes_name">');
    $(this).css('display', 'none');
  }
});
