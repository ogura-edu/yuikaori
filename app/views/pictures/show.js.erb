$('.modal-body').html("<%= escape_javascript(render 'show') %>");
$('.modal-footer').html("<%= escape_javascript(link_to 'Edit', edit_picture_path(@picture), class: 'btn btn-primary') %>");
$('#show_modal').modal('show');
