- provide :title, '削除申請'

= form_tag deletion_request_media_contents_path, method: :patch do
  #masonry-container.transitions-enabled.centered.clearfix
    .sizer
    .page
      = render @media_contents
  = paginate @media_contents
  = render 'footer'
