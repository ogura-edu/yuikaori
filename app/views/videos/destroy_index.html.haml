- provide :title, '削除申請'

= form_tag multiple_videos_path do
  #masonry-container.transitions-enabled.centered.clearfix
    .sizer
    .page
      = render @videos
  = paginate @videos
  = render 'footer'
