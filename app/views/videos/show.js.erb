$('.modal-body').html("<%= escape_javascript(render 'show') %>");
$('.modal-footer').html(" <%= escape_javascript(link_to 'Edit', edit_video_path(@video), class: 'btn btn-primary') %> <button class='btn' data-dismiss='modal' aria-hidden='true'>Close</button> ");
$('#show_modal').modal('show');
